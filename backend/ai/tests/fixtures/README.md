Fixtures for AI tests.

Suggested structure:
- `images/`: sample document/selfie images for integration tests.
- `ocr_text/`: raw OCR text samples.
- `expected_outputs.json`: expected extraction/classification outputs.

Keep all fixture files anonymized and non-production.

